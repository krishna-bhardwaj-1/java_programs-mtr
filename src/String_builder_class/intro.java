package String_builder_class;

public class intro {
    public static void main(String[] args) {
        /*
        1). It present in java.lang package
        2). it is used to create mutable(modified) string in JAVA
        3). it is same as stringbuffer class except stringbuffer is thread safe and it is unsafe
        Methods for String Builder Class same as stringbuffer class:
        capacity(); append();   insert();   delete();   replace();  deleteCharAt();   setCharAt();
        reverse();  toString(); Length();   charAt();
        */
    }
}
